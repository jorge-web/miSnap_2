
// sets the path to the web worker
var mitekWorkerPath = "../src/mitekMobileWeb.js"; // the path to mitekMobileWeb.js (e.g. http://localhost/MitekMobileWebAPI/src/mitekMobileWeb.js)

// sample dl_front captureParams
var frontCaptureParams = {
    maxImageSize: 1920,
    jpegEncodingQuality: 70
};

// sample dl_back captureParams
var backCaptureParams = {
    maxImageSize: 1920,
    jpegEncodingQuality: 70
};